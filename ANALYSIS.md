# Этап 2. Анализ сущностей

Таблица заполнена **до** выбора хранилища — иначе выбор подгоняется под уже понравившийся инструмент.

| № | Сущность | Часто читают или пишут? | Нужны транзакции? | Нужны JOIN / связи? | Объём данных? | Требования к задержке? | Гибкость схемы? |
|---|---|---|---|---|---|---|---|
| 1 | **Пользователи** | Читают почти на каждом запросе, пишут редко | Да: регистрация + назначение роли одной операцией | Да: роли, права, авторство курсов | Малый: 10⁵–10⁶ строк, десятки МБ | Проверка прав < 10 мс | Низкая, схема стабильна |
| 2 | **Сессии и токены** | Читают очень часто, пишут при входе | Нет | Нет, только ссылка на `user_id` | Малый, равен числу активных сессий | < 1–5 мс | Низкая, 3–5 полей |
| 3 | **Курсы и уроки** | Читают часто (каталог), пишут редко (авторы) | Слабые: публикация должна быть атомарной | Да для метаданных (автор, категория), нет для тела курса | Малый по строкам, «тяжёлый» по документам | 50–200 мс, кэшируемо | Высокая: разные типы курсов — разный набор полей, вложенность 4 уровня |
| 4 | **Прогресс по курсам** | Пишут часто, читают ещё чаще | Да: результат теста → зачёт урока → сертификат | Да: `users`, `lessons`, `courses` | Большой: users × lessons, 10⁷–10⁸ строк | Чтение текущего состояния < 50 мс | Низкая, схема жёсткая |
| 5 | **Поиск по курсам** | Только читают | Нет | Нет, документ денормализован | Малый (равен каталогу), но индекс объёмный | < 200 мс | Средняя: маппинг меняется редко, но требует переиндексации |
| 6 | **Форум / обсуждения** | Читают чаще, чем пишут | Слабые: ответ + счётчик, действия модератора | Да: дерево ответов, авторы, темы | Средний: 10⁶–10⁷ сообщений | 200–500 мс допустимо | Низкая |
| 7 | **Логи действий** | Пишут потоком, читают редко | Нет | Нет, события денормализованы | Огромный: 10⁹+ строк в год | Запись асинхронная, потеря единичных событий терпима | Средняя: набор полей события расширяется |
| 8 | **Аналитика и отчёты** | Пишут витрины, читают для проверки | Нет | Агрегаты + JOIN с небольшими справочниками | Вся история | Секунды–минуты, свежесть T+15 мин | Низкая, витрины фиксированы |
| 9 | **Рекомендации курсов** | Читают часто (выдача), пишут при пересчёте | Нет | Связи «пользователь → курс» важны при расчёте | Средний: топ-N на пользователя | Выдача < 50 мс | Низкая: ключ → список id |
